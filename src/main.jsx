import React from 'react';
import ReactDOM from 'react-dom/client';
import BadgersDraftBoard from './BadgersDraftBoard';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BadgersDraftBoard />
  </React.StrictMode>
);
